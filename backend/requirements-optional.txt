# Optional: install after requirements-unified.txt if you need LangChain/MCP.
# Fyers: pip install -r requirements-fyers.txt (see that file for Python 3.13 note).
# Install (PowerShell):
#   $py = "<project>\backend\.venv\Scripts\python.exe"
#   & $py -m pip install -r requirements-unified.txt
#   & $py -m pip install -r requirements-optional.txt
# Do NOT use bare `pip install` — it installs to user dir, not the venv.

# Keep numpy>=2 so pip does not downgrade to numpy 1.x (no wheel for Python 3.13)
numpy>=2.0.0
# aiohttp: LangChain needs >=3.8.3, Fyers needs ==3.9.3. Install Fyers separately to get 3.9.3.
# If installing Fyers, it will set aiohttp==3.9.3 which satisfies langchain-community's >=3.8.3 requirement.
aiohttp>=3.8.3,<4.0.0

# LangChain stack (>=0.3.27 to match langchain-community; numpy 2 compatible)
langchain-core>=0.3.27,<0.4
langchain>=0.3.27,<0.4
langchain-community>=0.3.27,<0.4
langgraph>=0.0.15,<0.3
openai>=1.109.1,<3.0.0